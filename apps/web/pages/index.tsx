import type { NextPage } from 'next';
import MainContainer from '../components/MainContainer';

const Home: NextPage = () => {
  return (
    <MainContainer>
    </MainContainer>
  );
};

export default Home;
